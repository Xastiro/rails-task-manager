<h1>List of all tasks</h1>

<ul>
  <% @tasks.each do |task|%>
  <li><%= task.title %> | <%= task.details %> | <%= task.completed %> | <%= link_to "Details", task_path(task.id) %></li>
  <% end %>
</ul>
